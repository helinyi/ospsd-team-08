[project]
name = "calendar-integration"
version = "0.1.0"
requires-python = ">=3.13"
dependencies = [
    "calendar-client-api",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"