version: 2.1

jobs:
  lint:
    docker:
      - image: cimg/python:3.13.3
    steps:
      - checkout
      - run:
          name: Install uv
          command: |
            curl -LsSf https://astral.sh/uv/install.sh | sh
            echo 'export PATH="$HOME/.local/bin:$PATH"' >> "$BASH_ENV"
      - run:
          name: Install dependencies
          command: uv sync --all-packages --group dev
      - run:
          name: Lint (ruff)
          command: uv run ruff check --config pyproject.toml .
      - run:
          name: Type check (mypy)
          command: uv run mypy components/

  test:
    docker:
      - image: cimg/python:3.13.3
    steps:
      - checkout
      - run:
          name: Install uv
          command: |
            curl -LsSf https://astral.sh/uv/install.sh | sh
            echo 'export PATH="$HOME/.local/bin:$PATH"' >> "$BASH_ENV"
      - run:
          name: Install dependencies
          command: uv sync --all-packages --group dev
      - run:
          name: Run unit tests
          command: |
            mkdir -p test-results/unit
            uv run pytest components/discord_client_impl/tests/ \
              components/discord_service/tests/ \
              components/chat_client_adapter/tests/ \
              --junitxml=test-results/unit/results.xml \
              --cov=discord_client_impl \
              --cov=discord_service \
              --cov=chat_client_adapter \
              --cov-report=xml:coverage.xml \
              --cov-report=html:htmlcov
      - run:
          name: Run integration & e2e tests
          command: |
            mkdir -p test-results/integration
            uv run pytest tests/ \
              --junitxml=test-results/integration/results.xml \
              --cov=discord_client_impl \
              --cov=chat_client_api \
              --cov-append \
              --cov-report=xml:coverage.xml \
              --cov-report=html:htmlcov
      - store_test_results:
          path: test-results
      - store_artifacts:
          path: htmlcov
          destination: coverage-report

workflows:
  main:
    jobs:
      - lint
      - test
